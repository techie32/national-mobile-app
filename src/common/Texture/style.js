import { StyleSheet } from "react-native";

export default StyleSheet.create({
  texture: {
    justifyContent: "center",
    flex: 1,
    resizeMode: "cover",
    width: "100%",
    resizeMode: "cover",
    position: "absolute",
    height: "100%",
  },
});
